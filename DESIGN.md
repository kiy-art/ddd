# DESIGN.md — サイトリニューアル記録

「ゴルフ用品の価格比較サイト」から「PAR.」というブランドへの全面リニューアル記録です。
目標は「高級ゴルフブランド × Apple × スタイリッシュな金融アプリ × AI」の空気感を持つ、
初見で価格比較サイトと分かってしまわない体験を作ることでした。

## 1. リニューアル前の問題点

- **主役が不在**：全商品が同じ重みでグリッド表示され、「今日これを買うべき」という
  強いメッセージがなかった。
- **配色が安売りサイト的**：値下がり=赤、それ以外=グレーという典型的な価格比較サイトの
  配色で、狙う高級感と逆行していた。
- **タイポグラフィがフラット**：見出しも本文も同じサンセリフで、情報の階層・世界観が
  弱かった。
- **独自UIの欠如**：買い時判定が定型的なバッジのみで、「この数字を見るために来る」と
  思わせる要素がなかった。
- **ブランドが存在しない**：ロゴ・favicon・OGP画像がなく、「個人が作ったサイト」感が
  強かった。

## 2. デザイン方針

| 項目 | 方針 |
|---|---|
| ブランド名 | **PAR.**（ゴルフの「基準スコア」と金融の「額面価格」の両方の意味を持つ言葉を採用） |
| カラー | ベースは温かみのあるオフホワイト(`#FAF9F6`)。アクセントは深いフェアウェイグリーン (`#0F3D2E`) 1色＋控えめな真鍮色ゴールド(`#A9835B`)。赤/青の価格比較色は廃止し、AI BUY SIGNALの単一トーンスケールに統一 |
| タイポグラフィ | 見出し＝`Fraunces`（オプティカルサイズ可変のエディトリアル系セリフ）、本文/UI＝`Geist`。2階層構成で情報の格を分けた |
| 独自UI | **AI BUY SIGNAL** — `conic-gradient`によるドーナツ型ゲージで0〜99のスコアを表示。バッジではなくデータビジュアライゼーションとして機能し、SNSでスクリーンショットしても意味が伝わる設計 |
| ページ構成 | Hero → Today's Best Buy(厳選5商品) → Browse All(絞り込み) → How It Works → Newsletter → Footer というストーリー性のある縦の流れ |
| アニメーション | ページロード時のフェードアップ(スクロール連動、IntersectionObserver)、数字のカウントアップ、価格チャートの描画アニメーション(`stroke-dashoffset`)、カードのホバー動作のみに限定。装飾目的のアニメーションは入れていない |

## 3. 実装した主な変更

- `app/globals.css` … CSSカスタムプロパティによるデザイントークン一式（ライト/ダーク両対応）、
  `Fraunces`/`Geist`フォント変数、フェード・描画アニメーションのkeyframe
- `components/Logo.tsx` / `app/icon.svg` … ブランドマーク（AI BUY SIGNALのゲージと
  同じ「円弧」モチーフを流用し、ロゴ自体がプロダクトの核心を表すデザインに）
- `app/opengraph-image.tsx` … 動的OGP画像生成。SNS共有時にブランドカラーの画像が
  表示されるように
- `components/AiBuySignal.tsx` … 独自のスコアゲージコンポーネント。`price_change_percent`
  から表示用スコア(0〜99)を算出（既存のDB/APIには一切手を加えていない、表示専用の
  計算）
- `components/ProductCard.tsx` … エディトリアルなカードデザインに刷新。画像を大きく、
  価格差分を明確に、AI BUY SIGNALを主役に
- `components/PriceHistoryChart.tsx` … Catmull-Rom補間による滑らかな曲線、グラデーション
  塗り、`pathLength`トリックによる軽量な描画アニメーション
- `app/page.tsx` … Hero/Today's Best Buy/Browse All/How It Works/Newsletterの
  ストーリー構成に全面刷新
- `app/products/[slug]/page.tsx` … Price History・Why AI Thinks This Is a Good Buy
  の2セクション構成に再設計
- `components/SafeProductImage.tsx` … 画像URLの読み込みに失敗した場合、安っぽい
  「No Image」表示ではなくカテゴリアイコンに自動フォールバック
- 管理画面（`/admin/*`）は機能を変えず、新しいカラートークンのみ適用（ブランド訴求の
  対象外のため、構造は現状維持）
- 実商品画像：楽天市場商品検索APIのレスポンスに含まれる出品者自身の商品写真を
  `pipeline.fetch_rakuten_prices`で取得し、`image_url`が未設定の商品にのみ反映
  （手動で設定した画像は上書きしない）。同時に写真の出典である楽天商品ページを
  `affiliate_url`にも設定し、画像と購入リンクの参照先を一致させている。画像が
  取得できない商品は引き続きカテゴリアイコン表示（`app/disclaimer/page.tsx`に
  方針を明記）

## 4. レスポンシブ検証

Playwright（Chromium）でモバイル(390×844) / タブレット(834×1194) / デスクトップ
(1440×900)の3サイズを実際にレンダリングして確認しました。

検証中に見つけて修正した実バグ：

- `CountUp`コンポーネントが開発モードのReact Strict Mode下でアニメーションを
  1度も開始できない実装ミスがあり、統計数値が常に「0」のまま止まっていた
  （`useRef`による多重起動防止フラグが、Strict Modeのeffect二重実行と噛み合わず、
  アニメーションフレームが起動前にキャンセルされていた）。ガードを削除し、
  `value`の変化だけで再実行される素直な実装に修正
- 画像URLが無効な場合に壊れた画像アイコンが表示される問題 → `SafeProductImage`で
  カテゴリアイコンへの自動フォールバックを追加

なお、検証中に「フルページスクリーンショットで一部セクションが真っ白に見える」
「ヘッダーが二重に見える」という現象がありましたが、これはPlaywrightの
フルページ撮影方式が `position: sticky` や `IntersectionObserver` と干渉する
既知の制約によるもので、実際のブラウザでの通常スクロール時には発生しないことを
個別に確認済みです。

## 5. 今後さらに改善できる点

- **商品ごとのOGP画像**：現在はサイト共通のOGP画像のみ。将来的には商品ごとに
  価格・AI BUY SIGNALスコアを埋め込んだ動的OGP画像を生成すると、SNSシェア時の
  訴求力がさらに上がる
- **Price Dropsティッカー**：ブリーフにあった横スクロールの値下がりティッカーは
  スコープの都合で見送り。商品数が増えてきたら追加を検討
- **ダークモード切り替えUI**：現状は`prefers-color-scheme`依存のみ。手動トグルは
  未実装
- **Newsletter機能**：現在はUIのみで、実際のメール収集・送信機能は未実装（正直に
  「準備中」と表示）。実装する場合はメール配信サービスとの連携が必要

## 6. 「AI特有さ」の排除（STEP36、2026-09-25）

社長から「AI特有のグラデーション、とりあえず置いたようなカード型のレイアウト、
不自然な余白を完全に排除してほしい」という依頼を受けた。実装前にコードとレンダリング結果を
両方確認したところ、装飾目的の色グラデーションはそもそも1つも存在しなかった（`gradient`は
AI BUY SIGNALの`conic-gradient`ゲージと写真コラージュのフェードマスクのみ）。実際の「AIが
作った感」の正体は次の3点だった。

1. **絵文字ラベルの多用**：🔥📉💰🔮 が見出し・バッジ・ラベルに17箇所で使われていた
   （生成AIが書くマーケティングコピーの典型的な特徴）。
2. **同一パターンの白カードの連発**：商品詳細ページだけで「PRICE HISTORY」「PRICE
   FORECAST」「LONG-TERM TREND」「TIMELINE」「SEASONAL PATTERN」「STORE
   COMPARISON」「PRICE ALERT」「FAQ」「DATA SOURCE」の9つが、全く同じ「大文字
   トラッキングラベル＋見出し＋白い角丸ボックス（`rounded-2xl border bg-card`）」
   というテンプレートの繰り返しだった（`rounded-2xl`/`rounded-full`が87箇所）。
3. **バッジの氾濫**：FACT/FORECAST/AI推測という塗りつぶしピル型バッジがあちこちに
   散在し、ダッシュボードツール感を強めていた。

**対応**：
- 商品詳細ページの9つの白カードのうち、内容的に関連する「価格推移チャート」
  「長期価格推移（月次）」「いつ安くなりそう？（タイムライン）」の3つを1つのカードに
  統合（内部は`border-t`の区切り線のみ）。「価格予測」「FAQ」「データについて」
  「価格データからわかること」の4つはカードの枠自体を撤去し、`/guides`の各セクション
  と同じ「見出し＋区切り線のみ」のプレーンな構成に変更。実際に重要な変換導線を持つ
  「価格推移」「店舗比較」「値下がり通知登録」の3つだけがカードとして残る形にした
  （9枚→3枚）。
- サイト全体（トップページ・カテゴリページ・比較ページ・商品カード・特集ガイド等）
  から装飾目的の絵文字17箇所を削除。意味を持っていた予測信頼度の🟢🟡⚪だけは、情報を
  失わないよう、AI BUY SIGNALゲージと同じ`--signal-high/mid/low`カラートークンを
  使った小さな色付きドットに置き換えた。
- FACT/FORECAST/AI推測の塗りつぶしピルバッジを、色付きの小さいテキストラベル
  （「実績」「予測」）に統一し、視覚的な主張を弱めた。

WebSearch等の外部データ取得は発生させず、既存のPlaywright実機検証で「絵文字ゼロ・
コンソールエラーゼロ」を商品詳細・トップ・カテゴリ・比較ページで確認した。

## 7. 「Fairway Terminal」全面刷新（STEP50、2026-09-26）

**コンセプト**：「TradingView / Bloomberg のデータ分析感」×「高級ゴルフブランドのモダンな静けさ」。
- 読む面は、明るくコントラストの高い「ミスト色の紙」（`--background: #f3f5f4`、文字は黒曜石色 `#0a100d`）にする。商品写真（白背景が多い）がきれいに見えるため。
- データを見せる面（ヒーロー等）は、黒曜石色の「ターミナルパネル」（`.terminal-panel`：うっすらした計測グリッド＋エメラルドの光）にする。
- 「行動してよい／お得な価格」を示す色は、フェアウェイを思わせるエメラルド1色（`--brand: #047857`、明るい版 `#10b981`、最も深い色 `--fairway: #052e16`）に絞った。
- 旧ブランド色（テラコッタのオレンジ）は全て置き換えた。対象はファビコン、ロゴ、SNS用のカード画像。

**アクセシビリティ上の判断**：1つのエメラルドで「白文字ボタン」と「黒背景上のエメラルド文字」の両方のコントラスト基準を満たすことはできない。そのため `--on-brand`（ブランド色の面に載せる文字色：ライトでは白、ダークでは黒曜石色）を新設し、ブランド色のボタンの文字を全てこれに置き換えた。ライトモードの `#047857` は白文字で5.5:1、ダークモードの `#10b981` は黒曜石色の文字で約8.7:1。

**タイポグラフィ**：
- 見出し・本文（英字）は Geist。
- 日本語は Zen Kaku Gothic New。Windows標準のメイリオ等に依存せず、見出しで上品に見える和文ゴシック。実際に使う文字の範囲だけを読み込む。
- 数字（価格・%・スコア）は JetBrains Mono の等幅数字（`.font-num`：`tabular-nums` とスラッシュ付きゼロ）。価格の桁がそろい、分析ツールらしく見える。価格・スコアの表示20か所に適用した。
- 遊びの強いセリフ体だった Fraunces は、「分析ツール感」に合わないため廃止した。
- 商品名は太字に、補助情報は薄くし、視線の優先順位をはっきりさせた。

**コンポーネント**：
- **買い時スコア**：買い時の判定はエメラルドのグラデーションリングと発光。「今が買い時」はゆっくり明滅するように光る。様子見・待つのが無難は、目立たせないため光らせない。数字は等幅書体。
- **価格推移グラフ**：線に深い緑→明るいエメラルドのグラデーションと柔らかい発光を付け、最新価格の点から波紋が広がるようにした。マウスやタッチを重ねると十字線、光る点、日付と価格の吹き出しが出る。期間切り替えは TradingView 風の切り替えボタン。
- **商品カード**（`.card-lux`）：通常は細い境界線。ホバー時に境界線がエメラルドのグラデーションに変わり、3px浮き上がって緑がかった柔らかい影が付く。
- **「過去最安値圏」バッジ**：点滅するライブドットと発光を付けた。
- **ショップボタン**（`.btn-shop`：楽天・Yahoo!・トップのCTA）：深い緑→エメラルドの塗り。ホバーで光が横切り、矢印が前に進み、押すとわずかに縮む。補助ボタンは `.btn-ghost`。
- **NO IMAGE**：うっすらした計測グリッドの上に、細い枠線で囲んだカテゴリのアイコンと、等幅書体の小さな「NO IMAGE」。「壊れている」ではなく「意図した表示」に見えるようにした。
- **モバイル**：
  - 下部タブバーは半透明のすりガラス風にし、選択中のタブに発光するエメラルドの線を付けた。押したときの反応も付けた。
  - 「もっと」とハンバーガーメニューは、下からせり上がるボトムシートに統一した。つまみ付きで、Escや背景タップで閉じ、閉じるときも滑らかに下がる。開いている間は背後のページがスクロールしない。
- **スケルトン表示**：`app/loading.tsx`（一覧）、`app/products/[slug]/loading.tsx`（商品ページ）を新設した。実際のレイアウトと同じ形の、目に優しいきらめき表示。Renderの無料プランでバックエンドの起動が遅いときにも、画面が真っ白にならない。
- 動きは全て `prefers-reduced-motion` で止まる。独自クラスは `@layer components` に置き、Tailwindのユーティリティで上書きできるようにした。
