# DESIGN.md — サイトリニューアル記録

「ゴルフ用品の価格比較サイト」から「PAR.」というブランドへの全面リニューアル記録です。
目標は「高級ゴルフブランド × Apple × スタイリッシュな金融アプリ × AI」の空気感を持つ、
初見で価格比較サイトと分かってしまわない体験を作ることでした。

## 1. リニューアル前の問題点

- **主役が不在**：全商品が同じ重みでグリッド表示され、「今日これを買うべき」という
  強いメッセージがなかった。
- **配色が安売りサイト的**：値下がり=赤、それ以外=グレーという典型的な価格比較サイトの
  配色で、狙う高級感と逆行していた。
- **タイポグラフィがフラット**：見出しも本文も同じサンセリフで、情報の階層・世界観が
  弱かった。
- **独自UIの欠如**：買い時判定が定型的なバッジのみで、「この数字を見るために来る」と
  思わせる要素がなかった。
- **ブランドが存在しない**：ロゴ・favicon・OGP画像がなく、「個人が作ったサイト」感が
  強かった。

## 2. デザイン方針

| 項目 | 方針 |
|---|---|
| ブランド名 | **PAR.**（ゴルフの「基準スコア」と金融の「額面価格」の両方の意味を持つ言葉を採用） |
| カラー | ベースは温かみのあるオフホワイト(`#FAF9F6`)。アクセントは深いフェアウェイグリーン (`#0F3D2E`) 1色＋控えめな真鍮色ゴールド(`#A9835B`)。赤/青の価格比較色は廃止し、AI BUY SIGNALの単一トーンスケールに統一 |
| タイポグラフィ | 見出し＝`Fraunces`（オプティカルサイズ可変のエディトリアル系セリフ）、本文/UI＝`Geist`。2階層構成で情報の格を分けた |
| 独自UI | **AI BUY SIGNAL** — `conic-gradient`によるドーナツ型ゲージで0〜99のスコアを表示。バッジではなくデータビジュアライゼーションとして機能し、SNSでスクリーンショットしても意味が伝わる設計 |
| ページ構成 | Hero → Today's Best Buy(厳選5商品) → Browse All(絞り込み) → How It Works → Newsletter → Footer というストーリー性のある縦の流れ |
| アニメーション | ページロード時のフェードアップ(スクロール連動、IntersectionObserver)、数字のカウントアップ、価格チャートの描画アニメーション(`stroke-dashoffset`)、カードのホバー動作のみに限定。装飾目的のアニメーションは入れていない |

## 3. 実装した主な変更

- `app/globals.css` … CSSカスタムプロパティによるデザイントークン一式（ライト/ダーク両対応）、
  `Fraunces`/`Geist`フォント変数、フェード・描画アニメーションのkeyframe
- `components/Logo.tsx` / `app/icon.svg` … ブランドマーク（AI BUY SIGNALのゲージと
  同じ「円弧」モチーフを流用し、ロゴ自体がプロダクトの核心を表すデザインに）
- `app/opengraph-image.tsx` … 動的OGP画像生成。SNS共有時にブランドカラーの画像が
  表示されるように
- `components/AiBuySignal.tsx` … 独自のスコアゲージコンポーネント。`price_change_percent`
  から表示用スコア(0〜99)を算出（既存のDB/APIには一切手を加えていない、表示専用の
  計算）
- `components/ProductCard.tsx` … エディトリアルなカードデザインに刷新。画像を大きく、
  価格差分を明確に、AI BUY SIGNALを主役に
- `components/PriceHistoryChart.tsx` … Catmull-Rom補間による滑らかな曲線、グラデーション
  塗り、`pathLength`トリックによる軽量な描画アニメーション
- `app/page.tsx` … Hero/Today's Best Buy/Browse All/How It Works/Newsletterの
  ストーリー構成に全面刷新
- `app/products/[slug]/page.tsx` … Price History・Why AI Thinks This Is a Good Buy
  の2セクション構成に再設計
- `components/SafeProductImage.tsx` … 画像URLの読み込みに失敗した場合、安っぽい
  「No Image」表示ではなくカテゴリアイコンに自動フォールバック
- 管理画面（`/admin/*`）は機能を変えず、新しいカラートークンのみ適用（ブランド訴求の
  対象外のため、構造は現状維持）
- 実商品画像：楽天市場商品検索APIのレスポンスに含まれる出品者自身の商品写真を
  `pipeline.fetch_rakuten_prices`で取得し、`image_url`が未設定の商品にのみ反映
  （手動で設定した画像は上書きしない）。同時に写真の出典である楽天商品ページを
  `affiliate_url`にも設定し、画像と購入リンクの参照先を一致させている。画像が
  取得できない商品は引き続きカテゴリアイコン表示（`app/disclaimer/page.tsx`に
  方針を明記）

## 4. レスポンシブ検証

Playwright（Chromium）でモバイル(390×844) / タブレット(834×1194) / デスクトップ
(1440×900)の3サイズを実際にレンダリングして確認しました。

検証中に見つけて修正した実バグ：

- `CountUp`コンポーネントが開発モードのReact Strict Mode下でアニメーションを
  1度も開始できない実装ミスがあり、統計数値が常に「0」のまま止まっていた
  （`useRef`による多重起動防止フラグが、Strict Modeのeffect二重実行と噛み合わず、
  アニメーションフレームが起動前にキャンセルされていた）。ガードを削除し、
  `value`の変化だけで再実行される素直な実装に修正
- 画像URLが無効な場合に壊れた画像アイコンが表示される問題 → `SafeProductImage`で
  カテゴリアイコンへの自動フォールバックを追加

なお、検証中に「フルページスクリーンショットで一部セクションが真っ白に見える」
「ヘッダーが二重に見える」という現象がありましたが、これはPlaywrightの
フルページ撮影方式が `position: sticky` や `IntersectionObserver` と干渉する
既知の制約によるもので、実際のブラウザでの通常スクロール時には発生しないことを
個別に確認済みです。

## 5. 今後さらに改善できる点

- **商品ごとのOGP画像**：現在はサイト共通のOGP画像のみ。将来的には商品ごとに
  価格・AI BUY SIGNALスコアを埋め込んだ動的OGP画像を生成すると、SNSシェア時の
  訴求力がさらに上がる
- **Price Dropsティッカー**：ブリーフにあった横スクロールの値下がりティッカーは
  スコープの都合で見送り。商品数が増えてきたら追加を検討
- **ダークモード切り替えUI**：現状は`prefers-color-scheme`依存のみ。手動トグルは
  未実装
- **Newsletter機能**：現在はUIのみで、実際のメール収集・送信機能は未実装（正直に
  「準備中」と表示）。実装する場合はメール配信サービスとの連携が必要
