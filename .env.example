# This is a reference only. Backend and frontend each read their own .env file:
#   backend/.env   (copy from backend/.env.example)
#   frontend/.env.local (copy from frontend/.env.example)

# --- backend/.env ---
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/golf_deals
ANTHROPIC_API_KEY=sk-ant-xxxx
CLAUDE_MODEL=claude-sonnet-5
ADMIN_API_TOKEN=change-me-to-a-random-secret
CORS_ORIGINS=http://localhost:3000
RAKUTEN_APP_ID=
RAKUTEN_ACCESS_KEY=
RAKUTEN_AFFILIATE_ID=
YAHOO_CLIENT_ID=
YAHOO_AFFILIATE_ID=
RESEND_API_KEY=
RESEND_FROM_EMAIL=PAR. <onboarding@resend.dev>
SITE_URL=https://par-gear.com
# X (Twitter) API - OAuth 1.0a credentials for the site's own posting
# account. All four required together; posting is a no-op until then.
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# --- frontend/.env.local ---
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_AMAZON_AFFILIATE_TAG=
